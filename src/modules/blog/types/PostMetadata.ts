import { FeaturedImage } from './FeaturedImage'

export type PostMetadata = {
  author: string
  description: string
  createdAt: string
  title: string
  slug: string
  featuredImage: FeaturedImage
}
